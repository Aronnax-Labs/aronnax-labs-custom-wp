/**
 * Deprecated versions of the Hero Block
 * 
 * Important: Never modify existing deprecated entries!
 * When you change the save() function, add the OLD version here.
 * This ensures backward compatibility with existing content.
 */

const deprecated = [];

export default deprecated;
